{
  "name": "backend",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "build": "go build -o bin/server .",
    "dev": "go run .",
    "clean": "rm -rf bin/",
    "test": "go test ./...",
    "tidy": "go mod tidy",
    "deps": "go mod download"
  }
}
